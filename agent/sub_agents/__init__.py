# This package is auto-discovered by agent.py.
# Each .py file here should expose a module-level variable named `agent`
# (an LlmAgent instance) to be included as a sub-agent.
#
# Example file layout (my_sub_agent.py):
#
#   from google.adk.agents import LlmAgent
#   agent = LlmAgent(name="my_sub_agent", ...)
